/**
 * Layout del flujo de onboarding.
 * Sin sidebar — solo la página centrada.
 */
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
